import GetToppings from './GetToppings';

describe('GetToppings', () => {
  describe('.execute()', () => {
    it('TODO', () => {
      // eslint-disable-next-line @typescript-eslint/no-unused-vars
      const useCase = new GetToppings();
      // ...
    });
  });
});
